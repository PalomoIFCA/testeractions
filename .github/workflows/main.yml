name: GitHub Actions SQAaaS
run-name: ${{ github.actor }} is using SQAaaS to evaluate ${{ github.repository }}
on: [push]
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - run: echo "🎉 The job was automatically triggered by a ${{ github.event_name }} event."
      - run: echo "🐧 This job is now running on a ${{ runner.os }} server hosted by GitHub!"
      #- name: lista1
      #  run: |
      #    ls ${{ github.workspace }}
      #- run: echo ls ${{ github.workspace }}
      #- run: echo ${{ github.workspace }}
      
      #- run: echo "🖥️ ssstart of list on the runner."
      #- name: List files in the repository
      #  run: |
      #    ls ${{ github.workspace }}
      #- run: echo "🍏 end of list."
      
      - run: echo "🔎 The name of your branch is ${{ github.ref }} and your repository is ${{ github.repository }}."
      - name: Check out repository code
        uses: actions/checkout@v4
      - name: store output test
        run: |
          export INPUT_STORING=$(python helloworld.py)
          echo 'the test:'
          echo $INPUT_STORING  
      - run: echo "💡 The ${{ github.repository }} repository has been cloned to the runner."
      - run: echo "🖥️ The workflow is now ready to test your code on the runner."
      #- name: List files in the repository
      #  run: |
      #    ls ${{ github.workspace }}
      #- run: python helloworld.py
      - run: |
             export $INPUT_STORE=$(python .github/workflows/request.py --repo ${{ github.repository }})
             echo Iit works
             echo INPUT_STORE
      - run: echo "🍏 This job's status is ${{ job.status }}."
